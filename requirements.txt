# requirements.txt - Project Odysseus Python Dependencies
# 동적 적응형 페어 트레이딩 봇 의존성 패키지 목록

# =============================================================================
# 🐍 Python 버전 요구사항: Python 3.10+
# =============================================================================

# =============================================================================
# 📊 핵심 데이터 분석 및 수치 계산
# =============================================================================

# 데이터 분석 및 조작
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# 통계 분석 및 시계열
statsmodels>=0.14.0
arch>=6.2.0              # GARCH 모델
scikit-learn>=1.3.0      # 머신러닝 기본 라이브러리

# 머신러닝 전용
xgboost>=2.0.0           # 그래디언트 부스팅
lightgbm>=4.0.0          # 대안 부스팅 알고리즘 (선택적)

# 칼만 필터
pykalman>=0.9.5          # 칼만 필터 구현
filterpy>=1.4.5          # 고급 필터링 (대안)

# =============================================================================
# 💹 금융 데이터 및 거래소 연동
# =============================================================================

# 거래소 API
ccxt>=4.2.0              # 통합 거래소 API
python-binance>=1.0.19   # Binance 전용 (선택적 - 고급 기능용)

# 금융 데이터 분석
yfinance>=0.2.20         # Yahoo Finance 데이터
ta>=0.10.2               # 기술적 분석 지표
TA-Lib>=0.4.28           # 고급 기술적 분석 (선택적)

# =============================================================================
# 🗄️ 데이터베이스 및 스토리지
# =============================================================================

# PostgreSQL (TimescaleDB)
psycopg2-binary>=2.9.7   # PostgreSQL 드라이버
sqlalchemy>=2.0.0        # ORM 및 데이터베이스 추상화
alembic>=1.12.0          # 데이터베이스 마이그레이션

# 대안 데이터베이스 (선택적)
influxdb-client>=1.38.0  # InfluxDB 클라이언트

# =============================================================================
# 🌐 웹 및 API 통신
# =============================================================================

# HTTP 클라이언트
requests>=2.31.0         # HTTP 요청
aiohttp>=3.8.5           # 비동기 HTTP 클라이언트
websockets>=11.0.3       # WebSocket 클라이언트

# REST API 서버 (대시보드용)
fastapi>=0.103.0         # 현대적인 웹 프레임워크
uvicorn>=0.23.0          # ASGI 서버
jinja2>=3.1.2            # 템플릿 엔진 (FastAPI 의존성)

# =============================================================================
# ⏰ 스케줄링 및 비동기 처리
# =============================================================================

# 작업 스케줄링
apscheduler>=3.10.4      # 고급 스케줄링
schedule>=1.2.0          # 간단한 스케줄링 (대안)

# 비동기 프로그래밍
asyncio-mqtt>=0.16.1     # 비동기 MQTT (선택적)
aiofiles>=23.2.1         # 비동기 파일 I/O

# =============================================================================
# 📝 로깅 및 모니터링
# =============================================================================

# 로깅
loguru>=0.7.2            # 현대적인 로깅
structlog>=23.1.0        # 구조화된 로깅 (선택적)

# 시스템 모니터링
psutil>=5.9.5            # 시스템 리소스 모니터링
py-cpuinfo>=9.0.0        # CPU 정보

# =============================================================================
# 🔧 유틸리티 및 설정 관리
# =============================================================================

# 환경 변수 및 설정
python-dotenv>=1.0.0     # .env 파일 로딩
pydantic>=2.4.0          # 데이터 검증 및 설정
pydantic-settings>=2.0.3 # 설정 관리

# 날짜 및 시간
python-dateutil>=2.8.2   # 날짜 파싱
pytz>=2023.3             # 타임존 처리

# 암호화 및 보안
cryptography>=41.0.0     # 암호화 기능
bcrypt>=4.0.1            # 패스워드 해싱

# =============================================================================
# 📊 시각화 및 리포팅 (선택적)
# =============================================================================

# 데이터 시각화
plotly>=5.17.0           # 인터랙티브 차트
matplotlib>=3.7.2        # 기본 차트
seaborn>=0.12.2          # 통계 시각화

# 리포팅
fpdf2>=2.7.5             # PDF 생성
openpyxl>=3.1.2          # Excel 파일 처리

# =============================================================================
# 🧪 테스팅 및 개발 도구
# =============================================================================

# 테스팅
pytest>=7.4.0           # 테스트 프레임워크
pytest-asyncio>=0.21.1  # 비동기 테스트
pytest-cov>=4.1.0       # 커버리지 측정
faker>=19.6.0            # 테스트 데이터 생성

# 코드 품질
black>=23.7.0            # 코드 포맷팅
flake8>=6.0.0            # 린팅
mypy>=1.5.0              # 타입 체킹
pre-commit>=3.4.0        # Git hook 관리

# =============================================================================
# 🐳 배포 및 운영
# =============================================================================

# WSGI/ASGI 서버
gunicorn>=21.2.0         # WSGI 서버
uvloop>=0.17.0           # 고성능 이벤트 루프 (Linux/macOS)

# 프로세스 관리
supervisor>=4.2.5        # 프로세스 관리자 (선택적)

# =============================================================================
# 📡 알림 및 통신
# =============================================================================

# 텔레그램
python-telegram-bot>=20.5  # 텔레그램 봇 API
telepot>=12.7               # 경량 텔레그램 클라이언트 (대안)

# 이메일
smtplib2>=0.2.1          # 이메일 발송 (내장 모듈 확장)
email-validator>=2.0.0   # 이메일 유효성 검증

# 슬랙 (선택적)
slack-sdk>=3.22.0        # 슬랙 API

# =============================================================================
# 🔍 데이터 수집 및 파싱
# =============================================================================

# 웹 스크래핑 (선택적)
beautifulsoup4>=4.12.2   # HTML 파싱
selenium>=4.12.0         # 브라우저 자동화 (선택적)

# 데이터 파싱
lxml>=4.9.3              # XML 파싱
pyyaml>=6.0.1            # YAML 파싱
toml>=0.10.2             # TOML 파싱

# =============================================================================
# 🚀 성능 최적화 (선택적)
# =============================================================================

# 수치 계산 가속화
numba>=0.58.0            # JIT 컴파일 (고성능 수치 계산)
bottleneck>=1.3.7        # Pandas 가속화

# 메모리 최적화
memory-profiler>=0.61.0  # 메모리 프로파일링
pympler>=0.9             # 메모리 사용량 분석

# =============================================================================
# 🌍 지역화 및 국제화 (선택적)
# =============================================================================

# 다국어 지원
babel>=2.12.1            # 국제화 라이브러리

# =============================================================================
# 📦 패키지 버전 고정 (중요한 의존성들)
# =============================================================================

# 호환성이 중요한 패키지들은 특정 버전 범위로 고정
# pandas~=2.0.0           # 2.0.x 시리즈 내에서만 업데이트
# numpy~=1.24.0           # 1.24.x 시리즈 내에서만 업데이트
# ccxt~=4.2.0             # 4.2.x 시리즈 내에서만 업데이트

# =============================================================================
# 💡 설치 가이드
# =============================================================================

# 1. 가상환경 생성 및 활성화:
#    python -m venv odysseus_env
#    source odysseus_env/bin/activate  # Linux/macOS
#    odysseus_env\Scripts\activate     # Windows

# 2. 의존성 설치:
#    pip install -r requirements.txt

# 3. 추가 패키지 (필요시):
#    pip install ta-lib  # 별도 컴파일 필요
#    
# 4. TA-Lib 설치 (고급 기술적 분석용):
#    # Ubuntu/Debian:
#    sudo apt-get install libta-lib0-dev
#    pip install ta-lib
#    
#    # macOS:
#    brew install ta-lib
#    pip install ta-lib
#    
#    # Windows:
#    # https://github.com/mrjbq7/ta-lib에서 .whl 파일 다운로드 후 설치

# 5. 개발 환경 설정:
#    pip install -r requirements-dev.txt  # 개발용 추가 패키지

# =============================================================================
# ⚠️ 주의사항
# =============================================================================

# 1. Python 3.10+ 필수
# 2. 일부 패키지(TA-Lib)는 시스템 라이브러리 설치 필요
# 3. Windows에서는 Visual C++ Build Tools 필요할 수 있음
# 4. M1 Mac에서는 일부 패키지 호환성 확인 필요
# 5. 가상환경 사용 강력 권장